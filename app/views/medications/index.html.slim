- content_for :title, '使用している薬の入力'

.w-full.max-w-4xl.mx-auto
  = render 'shared/progress_bar'

  .my-8.px-4.space-y-3
    h3.text-center.text-xl.font-medium
      | 使用している薬の入力
    .text-sm
      p
        | 💡 ご利用方法
        br
        | 使用開始日を入力しなくても保存できます。薬名は入力する必要があります。

  #medications
    - @medications.each do |medication|
      = render 'medications/medication', medication: medication

  = turbo_frame_tag 'new_form' do
    - if @medications.empty?
      = render 'form', medication: Medication.new

  = link_to '＋',
            new_medication_path,
            data: { turbo_frame: 'new_form' },
            class: 'mx-auto flex items-center justify-center w-10 h-10 bg-[#5F7F67] hover:bg-[#4A6652] rounded-full text-xl text-white'

  = render 'shared/navigation', previous_path: products_path, next_path: allergies_path
  = render 'shared/resume_form_footer'
