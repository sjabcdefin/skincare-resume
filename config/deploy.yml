service: skincare-resume-app
image: sjabcdefin/skincare-resume-app
servers:
  web:
    - skincare-resume.com
proxy:
  ssl: true
  host: skincare-resume.com
  app_port: 3000
registry:
  username: sjabcdefin
  password:
    - KAMAL_REGISTRY_PASSWORD
builder:
  arch: amd64
env:
  clear:
    POSTGRES_USER: appuser
    POSTGRES_HOST: 133.167.73.173
    SOLID_QUEUE_DATABASE: queue
  secret:
    - RAILS_MASTER_KEY
    - POSTGRES_PASSWORD
    - OMNIAUTH_CLIENT_ID
    - OMNIAUTH_CLIENT_SECRET
    - ALERT_EMAIL_ADDRESS
    - MAIL_FROM_ADDRESS
    - SMTP_PASSWORD
ssh:
  user: tomoka
  port: 1201
